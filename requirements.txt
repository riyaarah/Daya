# Core Dependencies
uagents==0.8.1
openai==1.30.1

# Optional but useful
python-dotenv>=1.0.1     # For managing environment variables like API keys
requests>=2.31.0         # In case you later add external HTTP requests
